@layout('layouts/app')

@section('content')
  <section class="adonai-shell py-20 text-center">
    <p class="text-xs font-extrabold uppercase tracking-[.18em] text-adonai-primary">Adonai Thrift Store</p>
    <h1 class="mt-3 text-4xl font-black tracking-tight">The store is temporarily unavailable</h1>
    <p class="mx-auto mt-4 max-w-xl text-sm leading-6 text-adonai-muted">{{ notice || 'Please try again shortly or contact the Kampala store on ' + site.callNumber + '.' }}</p>
    <div class="mt-7 flex flex-wrap justify-center gap-3">
      <a class="adonai-btn-primary" href="/">Return to the store</a>
      <a class="adonai-btn-secondary" href="{{ site.whatsappLink }}">WhatsApp {{ site.whatsappNumber }}</a>
    </div>
  </section>
@endsection
